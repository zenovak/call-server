export const Glass = ({children}) => {
    return (
        <div
            className=" bg-white/5 backdrop-blur-sm"
        >
            {children}
        </div>
    );
}

export const Tint = ({children}) => {
    
}